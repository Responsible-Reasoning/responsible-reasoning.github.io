{
  "name": "Jekyll Site",
  "image": "mcr.microsoft.com/devcontainers/ruby:1-3",
  "features": {},
  "forwardPorts": [4000],
  "portsAttributes": {
    "4000": {
      "label": "Jekyll site",
      "onAutoForward": "openPreview"
    }
  },
  "postCreateCommand": "sudo rm -f /etc/apt/sources.list.d/yarn.list && sudo apt-get update && sudo apt-get install -y imagemagick && gem install bundler jekyll && if [ -f Gemfile ]; then bundle install; fi",
  "customizations": {
    "vscode": {
      "extensions": []
    }
  }
}
